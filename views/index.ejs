<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="og:description" content="mood tracker">
  <link rel="stylesheet" href="/static/style/index.css">
  <script src="/static/secret_cookie_clicker/index.js" async></script>
  <link rel="stylesheet" href="/static/secret_cookie_clicker/index.css">
  <title>mood tracker</title>
</head>
<body>
  <main>
    <div id="view_header">
      <div>
        <% if (user) { %>
          <a href="/<%= user.username %>">profile</a>
          &squarf; <a href="/settings">settings</a>
          &squarf; <a href="/auth/logout">logout</a>
        <% } else { %>
          <a href="/auth/login">login</a>
          &squarf; <a href="/auth/register">register</a>
        <% } %>
      </div>
      <h1 id="secret_cookie_clicker">mood tracker</h1>
      <div id="sccs"></div>
    </div>
    <% if (user) { %>
      <p>
        <% const feelings = ["good", "alright", "okay", "well", "happy"] %>
        <% const days = ["an awesome", "a good", "a happy", "a wonderful", "a great", "a nice"] %>
        
        <% const feeling = feelings[Math.floor(Math.random() * feelings.length)] %>
        <% const day = days[Math.floor(Math.random() * days.length)] %>

        <% const greetings = [
          `Hope you're having ${day} day`,
          `Hope you have ${day} day`,
          `Hope you're doing ${feeling}`,
          `Hope you're ${feeling}`,
          `Hope you feel ${feeling}`,
        ] %>

        <%= greetings[Math.floor(Math.random() * greetings.length)] %>,
        <a class="username" href="/<%= user.username %>">@<%= user.username %></a>!
      </p>
    <% } else { %>
      <p>A simple mood tracker made by <a target="_blank" href="https://zptr.cc/">zptr</a></p>
      <a style="font-size: 1.1em" class="gradient" href="/auth/register">Create your own profile!</a>
    <% } %>
    <hr>
    <p>
      The site is currently in development and is still unfinished.<br>
      Any help would be appreciated! >>> <a target="_blank" href="https://github.com/zptr1/mood-tracker">GitHub</a>
    </p>
  </main>
</body>
</html>