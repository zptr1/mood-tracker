<!DOCTYPE html>
<html lang="en">
<head>
  <% locals.mood = "Register" %>
  <%- include('../partials/head') %>

  <script src="/static/script/register.js" async defer></script>
  <% if (turnstile_site_key) { %>
    <script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
  <% } %>
</head>
<body>
  <main>
    <h1><a href="/">mood tracker</a></h1>
    <form action="/auth/register" method="post">
      <label for="username">Username</label>
      <input name="username" id="username" type="text" placeholder="Username" minlength="3" maxlength="32" pattern="^[a-z0-9_-]{3,32}$" required>
      
      <label for="password">Password</label>
      <input name="password" id="password" type="password" placeholder="Password" required>
      
      <p id="status_error" style="color:#ee5858"><%= locals.error || "" %></p>
      
      <% if (turnstile_site_key) { %>
        <div class="cf-turnstile" data-sitekey="<%= turnstile_site_key %>"></div>
      <% } %>
      
      <input type="submit" value="Register">
    </form>
    <p>Already have an account? <a href="/auth/login">Sign in</a></p>
  </main>
</body>
</html>